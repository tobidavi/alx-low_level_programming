#include <stdio.h>

/**
 *main - Print all number of base ten starting with zero
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int q;

	for (q = 0; q < 10; q++)
	{
		printf("%d", q);
	}
	printf("\n");
	return (0);
}
