#include <stdio.h>

/**
 *main - lowecase alphabet printing in reverse order
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	char q;

	for (q = 'z' ; q >= 'a' ; c--)
	{
		putchar(q);
	}
	putchar('\n');
	return (0);
}
